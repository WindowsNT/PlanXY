<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="WuiFET.MsaApps"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WuiFET"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TeachingTip
            x:Name="tt"
            Title="{x:Bind str1,Mode=OneWay}"
            Subtitle="{x:Bind str2,Mode=OneWay}">
            <TeachingTip.IconSource>
                <SymbolIconSource Symbol="Accept" />
            </TeachingTip.IconSource>
        </TeachingTip>



        <InfoBar x:Name="infoBar" Grid.Row="0" IsOpen="True"  Severity="Success"  IsClosable="False" Message="Απευθείας εισαγωγή δεδομένων από την Ηλεκτρονική Διακυβέρνηση του Μουσικού Σχολείου Αλίμου (www.msa-apps.com).">
        </InfoBar>

        <Button Grid.Row="2" Margin="15" Style="{StaticResource SuccessButtonStyle}" Click="OnMSAOpen" Content="Ερώτηση για κλειδί πρόσβασης στο www.msa-apps.com"></Button>
        

    </Grid>
</Page>
