<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="WuiFET.ResultRooms"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WuiFET"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>


        <InfoBar x:Name="infoBar" Grid.Row="0" IsOpen="True" Message="" Severity="Success"  IsClosable="False">
            <Grid VerticalAlignment="Stretch">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="{x:Bind txt(51);}" Margin="10,0,0,0" Grid.Column="0" VerticalAlignment="Center"/>
            </Grid>
        </InfoBar>


        <MenuBar HorizontalAlignment="Left" x:Name="menu" Grid.Row="1">
            <MenuBarItem Title="{x:Bind txt(116)}" >
                <MenuFlyoutItem Text="{x:Bind txt(51)}" Click="Export1" />

            </MenuBarItem>
        </MenuBar>


        <Grid x:Name="TC_SingleRoom" Grid.Row="2" Grid.Column="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="{x:Bind Resizer_VV1,Mode=OneWay}" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="2*" />
            </Grid.ColumnDefinitions>

            <ListView x:Name="Rooms_ListView" Grid.Row="0" Grid.Column="0" ItemsSource="{x:Bind Rooms_List,Mode=OneWay}" VerticalAlignment="Stretch"  HorizontalAlignment="Stretch" SelectionChanged="RoomChanged" SelectionMode="Single">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="local:Item">
                        <StackPanel Orientation="Horizontal" Margin="10" >
                            <SymbolIcon Symbol="Page2" />
                            <TextBlock Text="{x:Bind Name1,Mode=OneWay}" Margin="10,0,0,0"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <local:Resizer Grid.Column="1" Grid.Row="1" Margin="10" Grid.RowSpan="2"  Width="1" Minimum="-1000"   Maximum="1000" Orientation="Vertical"   Value="{x:Bind Resizer_V1,Mode=TwoWay}"/>

            <Grid Grid.Column="2" Grid.Row="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <local:DGDataGrid x:Name="gr1" Grid.Row="1" Grid.Column="0" ItemsSource="{x:Bind gr1_is,Mode=OneWay}" ColumnHeaders="{x:Bind gr1_ch,Mode=OneWay}" />

            </Grid>

        </Grid>



    </Grid>
</Page>
