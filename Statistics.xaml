<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="WuiFET.Statistics"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WuiFET"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid x:Name="MainGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>


        <InfoBar x:Name="infoBar" Grid.Row="0" IsOpen="True" Message="" Severity="Success"  IsClosable="False">
            <Grid VerticalAlignment="Stretch">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="{x:Bind txt(121);}" Margin="10,0,0,0" Grid.Column="0" VerticalAlignment="Center"/>
            </Grid>
        </InfoBar>

    
        <MenuBar HorizontalAlignment="Left" x:Name="menu1" Grid.Row="1" Visibility="{x:Bind IsM1,Mode=OneWay}">
            <MenuBarItem Title="{x:Bind txt(24)}" >
                <MenuFlyoutItem Text="{x:Bind txt(87)}" Click="TeachersHours" />
            </MenuBarItem>
            <MenuBarItem Title="{x:Bind txt(33)}" >
                <MenuFlyoutItem Text="{x:Bind txt(87)}" Click="LessonsHours" />
            </MenuBarItem>
            <MenuBarItem Title="{x:Bind txt(92)}" >
                <MenuFlyoutItem Text="{x:Bind txt(87)}" Click="ClassHours" />
            </MenuBarItem>
        </MenuBar>
        <MenuBar HorizontalAlignment="Left" x:Name="menu2" Grid.Row="1" Visibility="{x:Bind IsM2,Mode=OneWay}">
            <MenuBarItem Title="{x:Bind txt(24)}" >
                <MenuFlyoutItem Text="{x:Bind txt(102)}" Click="MaxSpan" />
                <MenuFlyoutItem Text="{x:Bind txt(199)}" Click="TotalGaps" />
            </MenuBarItem>
        </MenuBar>
        
        <Line Stroke="SteelBlue" Grid.Row="2" X1="0" Y1="0" HorizontalAlignment="Stretch"      X2="{Binding ActualWidth, ElementName=MainGrid}" Y2="0"      StrokeThickness="2"/>


        <local:DGDataGrid Grid.Row="3" LayoutUpdated="gr1_ly" x:Name="gr1"  ItemsSource="{x:Bind gr1_is,Mode=OneWay}" ColumnHeaders="{x:Bind gr1_ch,Mode=OneWay}" />




    </Grid>
</Page>
