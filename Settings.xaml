<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="WuiFET.Settings"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WuiFET"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">


    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <InfoBar x:Name="infoBar" Grid.Row="0" IsOpen="True" Grid.ColumnSpan="2" Message="" Severity="Success"  IsClosable="False">
            <Grid VerticalAlignment="Stretch">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="{x:Bind txt(126);}" Margin="10,0,0,0" Grid.Column="0" VerticalAlignment="Center"/>
            </Grid>
        </InfoBar>

        <TextBlock Text="Language" Margin="10" VerticalAlignment="Center" Grid.Row="1" Grid.Column="0"/>
        <ComboBox Margin="10" HorizontalAlignment="Stretch" Grid.Row="1" Grid.Column="1" SelectedIndex="{x:Bind WhatLanguage,Mode=TwoWay}" x:Name="LangCB">
            <ComboBoxItem Tag="9">English</ComboBoxItem>
            <ComboBoxItem Tag="1">Arabic - العربية</ComboBoxItem>
            <ComboBoxItem Tag="4">Chinese - 中文</ComboBoxItem>
            <ComboBoxItem Tag="12">French - Français</ComboBoxItem>
            <ComboBoxItem Tag="8">Greek - Ελληνικά</ComboBoxItem>
            <ComboBoxItem Tag="7">German - Deutsch</ComboBoxItem>
            <ComboBoxItem Tag="57">Hindi - हिन्दी</ComboBoxItem>
            <ComboBoxItem Tag="24">Romanian - Română</ComboBoxItem>
            <ComboBoxItem Tag="25">Russian - русский</ComboBoxItem>
            <ComboBoxItem Tag="10">Spanish - Espagnol</ComboBoxItem>
        </ComboBox>



    </Grid>
</Page>
