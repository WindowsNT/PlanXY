<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="WuiFET.ResultList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WuiFET"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">


    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <InfoBar x:Name="infoBar" Grid.Row="0" IsOpen="True" Message="" Severity="Success"  IsClosable="False">
            <Grid VerticalAlignment="Stretch">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="{x:Bind txt(75);}" Margin="10,0,0,0" Grid.Column="0" VerticalAlignment="Center"/>
            </Grid>
        </InfoBar>

        <ListView x:Name="ResultsList" Grid.Row="1" Grid.Column="0" ItemsSource="{x:Bind Results_List,Mode=OneWay}" VerticalAlignment="Stretch"  HorizontalAlignment="Stretch" SelectionMode="Extended" DoubleTapped="DoubleClick1">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:Item">
                    <StackPanel Orientation="Horizontal" Margin="10" >
                        <SymbolIcon Symbol="Accept"></SymbolIcon>
                        <TextBlock Text="{x:Bind Name1,Mode=OneWay}" Margin="10,0,0,0">
                            <TextBlock.ContextFlyout>
                                <MenuFlyout>
                                    <MenuFlyoutItem Text="{x:Bind txt(81);}" Click="OnResultSelected_View" />
                                    <MenuFlyoutSeparator/>
                                    <MenuFlyoutItem Text="MSA-APPS" Click="OnResultSelected_PostMSAAPPS" />
                                    <MenuFlyoutItem Text="{x:Bind txt(185);}" Click="OnResultSelected_ExportFET" />
                                    <MenuFlyoutSeparator/>
                                    <MenuFlyoutItem Text="{x:Bind txt(46);}" Click="OnResultSelected_Delete" />
                                </MenuFlyout>
                            </TextBlock.ContextFlyout>
                        </TextBlock>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>


    </Grid>
    
    
</Page>
