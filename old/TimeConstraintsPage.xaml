<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="WuiFET.TimeConstraintsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WuiFET"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Generic -->
        <InfoBar x:Name="infoBar" Grid.Row="0" IsOpen="True" Message="" Severity="Warning"  IsClosable="False">
            <Grid VerticalAlignment="Stretch">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="{x:Bind txt(19);}" Margin="10,0,0,0" Grid.Column="0" VerticalAlignment="Center"/>
            </Grid>
        </InfoBar>


        <MenuBar HorizontalAlignment="Left" x:Name="menu" Grid.Row="1">
            <MenuBarItem Title="{x:Bind txt(31)}" >
                <MenuFlyoutItem Text="{x:Bind txt(77)}" Click="TS_Teacher_NotAvailableTimes"/>
                <MenuFlyoutSeparator/>
                <MenuFlyoutSubItem Text="{x:Bind txt(100);}">
                    <MenuFlyoutItem Text="{x:Bind txt(96)}" Click="TS_Teacher_MaxGapsDay"/>
                    <MenuFlyoutItem Text="{x:Bind txt(104)}" Click="TS_Teacher_MinHoursDay"/>
                    <MenuFlyoutItem Text="{x:Bind txt(99)}" Click="TS_Teacher_MaxHoursDay"/>
                    <MenuFlyoutItem Text="{x:Bind txt(102)}" Click="TS_Teacher_MaxSpanDay"/>
                    <MenuFlyoutItem Text="{x:Bind txt(105)}" Click="TS_Teacher_MaxContinuousHours"/>
                </MenuFlyoutSubItem>
                <MenuFlyoutSubItem Text="{x:Bind txt(101);}">
                    <MenuFlyoutItem Text="{x:Bind txt(93)}" Click="TS_Teacher_MinDaysWeek"/>
                    <MenuFlyoutItem Text="{x:Bind txt(94)}" Click="TS_Teacher_MaxDaysWeek"/>
                    <MenuFlyoutItem Text="{x:Bind txt(97)}" Click="TS_Teacher_MaxGapsWeek"/>
                </MenuFlyoutSubItem>
                <MenuFlyoutSeparator/>
                <MenuFlyoutItem Text="{x:Bind txt(98)}" Click="TS_Teacher_NoWork2CD"/>
            </MenuBarItem>
            <MenuBarItem Title="{x:Bind txt(24)}" >
                <MenuFlyoutSubItem Text="{x:Bind txt(100);}">
                    <MenuFlyoutItem Text="{x:Bind txt(96)}" Click="TS_Teachers_MaxGapsDay"/>
                    <MenuFlyoutItem Text="{x:Bind txt(104)}" Click="TS_Teachers_MinHoursDay"/>
                    <MenuFlyoutItem Text="{x:Bind txt(99)}" Click="TS_Teachers_MaxHoursDay"/>
                    <MenuFlyoutItem Text="{x:Bind txt(102)}" Click="TS_Teachers_MaxSpanDay"/>
                    <MenuFlyoutItem Text="{x:Bind txt(105)}" Click="TS_Teachers_MaxContinuousHours"/>
                </MenuFlyoutSubItem>
                <MenuFlyoutSubItem Text="{x:Bind txt(101);}">
                    <MenuFlyoutItem Text="{x:Bind txt(93)}" Click="TS_Teachers_MinDaysWeek"/>
                    <MenuFlyoutItem Text="{x:Bind txt(94)}" Click="TS_Teachers_MaxDaysWeek"/>
                    <MenuFlyoutItem Text="{x:Bind txt(97)}" Click="TS_Teachers_MaxGapsWeek"/>
                </MenuFlyoutSubItem>
                <MenuFlyoutSeparator/>
                <MenuFlyoutItem Text="{x:Bind txt(98)}" Click="TS_Teachers_NoWork2CD"/>
            </MenuBarItem>
            <MenuBarItem Title="{x:Bind txt(92)}" >
                <MenuFlyoutItem Text="{x:Bind txt(77)}" Click="TS_Class_NotAvailableTimes"/>
            </MenuBarItem>
            <MenuBarItem Title="{x:Bind txt(52)}" >
                <MenuFlyoutItem Text="{x:Bind txt(77)}" Click="TS_Room_NotAvailableTimes"/>
            </MenuBarItem>
            <!--            <MenuBarItem Title="{x:Bind txt(24)}" >
            </MenuBarItem>-->
        </MenuBar>
        
        
        <!-- Teacher -->
        <Grid Visibility="{x:Bind WhatX(1);,Mode=OneWay}" x:Name="TC_SingleTeacher" Grid.Row="2" Grid.Column="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="{x:Bind Resizer_VV1,Mode=OneWay}" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="3*" />
            </Grid.ColumnDefinitions>

            <ListView Visibility="{x:Bind WhatFX(1);,Mode=OneWay}" x:Name="Teachers_ListView" Grid.Row="0" Grid.Column="0" ItemsSource="{x:Bind Teachers_List,Mode=OneWay}" VerticalAlignment="Stretch"  HorizontalAlignment="Stretch" SelectionChanged="TeacherChanged" SelectionMode="Single">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="local:Item">
                        <StackPanel Orientation="Horizontal" Margin="10" >
                            <SymbolIcon Symbol="Page2" />
                            <TextBlock Text="{x:Bind Name1,Mode=OneWay}" Margin="10,0,0,0">
                            </TextBlock>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <local:Resizer Grid.Column="1" Margin="10" Grid.RowSpan="2"  Width="1" Minimum="-1000"   Maximum="1000" Orientation="Vertical"   Value="{x:Bind Resizer_V1,Mode=TwoWay}"/>

            <!-- Teacher NotAvailableTimes -->
            <Grid Grid.Column="2" Margin="5" Grid.Row="0" Visibility="{x:Bind WhatY(1);,Mode=OneWay}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Grid Grid.Column="2" Margin="5" Grid.Row="0" Visibility="{x:Bind WhatY(1);,Mode=OneWay}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <InfoBar Grid.Row="0" IsOpen="True" Message="{x:Bind txt(77)}" Severity="Warning"  IsClosable="False">
                    </InfoBar>
                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <DropDownButton x:Name="dd1" Content="{x:Bind gr1_n1,Mode=OneWay}" Click="dd1Click" Grid.Column="0"  Margin="5" />
                        <TextBlock Grid.Column="1" Margin="5" VerticalAlignment="Center" Text="{x:Bind txt(84)}" />
                        <NumberBox x:Name="nb1" Grid.Column="2" HorizontalAlignment="Stretch" Margin="5" PlaceholderText="{x:Bind txt(84)}" Value="{x:Bind gr1_p,Mode=TwoWay}" Minimum="0" Maximum="100" SpinButtonPlacementMode="Inline" />
                    </Grid>
                    <local:DGDataGrid x:Name="gr1" Grid.Row="2" Grid.Column="0" ItemsSource="{x:Bind gr1_is,Mode=OneWay}" ColumnHeaders="{x:Bind gr1_ch,Mode=OneWay}" />
                </Grid>
            </Grid>


            <!-- Teacher Generic -->
            <Grid Grid.Column="2" Margin="5" Grid.Row="0" Visibility="{x:Bind WhatY(2);,Mode=OneWay}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Grid Grid.Column="2" Margin="5" Grid.Row="0" Visibility="{x:Bind WhatY(2);,Mode=OneWay}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <InfoBar Grid.Row="0" IsOpen="True" Message="{x:Bind zz21_n2,Mode=OneWay}" Severity="Warning"  IsClosable="False">
                    </InfoBar>
                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <CheckBox Grid.Column="0" IsChecked="{x:Bind zz21_A,Mode=TwoWay}" Content="{x:Bind txt(95)}"></CheckBox>
                        <DropDownButton Visibility="{x:Bind zz21_HasM,Mode=OneWay}" x:Name="zz21_dd" Content="{x:Bind zz21_n1,Mode=OneWay}" Click="zz21_Click" Grid.Column="1"  Margin="5" />
                        <TextBlock Visibility="{x:Bind zz21_HasP,Mode=OneWay}" Grid.Column="2" Margin="5" VerticalAlignment="Center" Text="{x:Bind txt(84)}" />
                        <NumberBox Visibility="{x:Bind zz21_HasP,Mode=OneWay}" x:Name="zb21" Grid.Column="3" HorizontalAlignment="Stretch" Margin="5" PlaceholderText="{x:Bind txt(84)}" Value="{x:Bind zz21_p,Mode=TwoWay}" Minimum="0" Maximum="100" SpinButtonPlacementMode="Inline" />
                    </Grid>
                    <local:DGDataGrid x:Name="zz21_gr" Grid.Row="2" Grid.Column="0" ItemsSource="{x:Bind zz21_is,Mode=OneWay}" ColumnHeaders="{x:Bind zz21_ch,Mode=OneWay}" />
                </Grid>
            </Grid>


        </Grid>


        <!-- Class -->
        <Grid Visibility="{x:Bind WhatX(2);,Mode=OneWay}" x:Name="TC_SingleClass" Grid.Row="2" Grid.Column="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="{x:Bind Resizer_VV1,Mode=OneWay}" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="3*" />
            </Grid.ColumnDefinitions>

            <ListView x:Name="Classes_ListView" Grid.Row="0" Grid.Column="0" ItemsSource="{x:Bind Classes_List,Mode=OneWay}" VerticalAlignment="Stretch"  HorizontalAlignment="Stretch" SelectionChanged="ClassChanged" SelectionMode="Single">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="local:Item">
                        <StackPanel Orientation="Horizontal" Margin="10" >
                            <SymbolIcon Symbol="Page2" />
                            <TextBlock Text="{x:Bind Name1,Mode=OneWay}" Margin="10,0,0,0">
                            </TextBlock>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <local:Resizer Grid.Column="1" Margin="10" Grid.RowSpan="2"  Width="1" Minimum="-1000"   Maximum="1000" Orientation="Vertical"   Value="{x:Bind Resizer_V1,Mode=TwoWay}"/>

            <!-- Class NotAvailableTimes -->
            <Grid Grid.Column="2" Margin="5" Grid.Row="0" Visibility="{x:Bind WhatY(1);,Mode=OneWay}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Grid Grid.Column="2" Margin="5" Grid.Row="0" Visibility="{x:Bind WhatY(1);,Mode=OneWay}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <InfoBar Grid.Row="0" IsOpen="True" Message="{x:Bind txt(77)}" Severity="Warning"  IsClosable="False">
                    </InfoBar>
                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <DropDownButton x:Name="dd2" Content="{x:Bind gr2_n1,Mode=OneWay}" Click="dd2Click" Grid.Column="0"  Margin="5" />
                        <TextBlock Grid.Column="1" Margin="5" VerticalAlignment="Center" Text="{x:Bind txt(84)}" />
                        <NumberBox x:Name="nb2" Grid.Column="2" HorizontalAlignment="Stretch" Margin="5" PlaceholderText="{x:Bind txt(84)}" Value="{x:Bind gr2_p,Mode=TwoWay}" Minimum="0" Maximum="100" SpinButtonPlacementMode="Inline" />
                    </Grid>
                    <local:DGDataGrid x:Name="gr2" Grid.Row="2" Grid.Column="0" ItemsSource="{x:Bind gr2_is,Mode=OneWay}" ColumnHeaders="{x:Bind gr2_ch,Mode=OneWay}" />
                </Grid>
            </Grid>
            
        </Grid>





        <!-- Room -->
        <Grid Visibility="{x:Bind WhatX(3);,Mode=OneWay}" x:Name="TC_SingleRoom" Grid.Row="2" Grid.Column="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="{x:Bind Resizer_VV1,Mode=OneWay}" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="3*" />
            </Grid.ColumnDefinitions>

            <ListView x:Name="Rooms_ListView" Grid.Row="0" Grid.Column="0" ItemsSource="{x:Bind Rooms_List,Mode=OneWay}" VerticalAlignment="Stretch"  HorizontalAlignment="Stretch" SelectionChanged="RoomChanged" SelectionMode="Single">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="local:Item">
                        <StackPanel Orientation="Horizontal" Margin="10" >
                            <SymbolIcon Symbol="Page2" />
                            <TextBlock Text="{x:Bind Name1,Mode=OneWay}" Margin="10,0,0,0">
                            </TextBlock>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <local:Resizer Grid.Column="1" Margin="10" Grid.RowSpan="2"  Width="1" Minimum="-1000"   Maximum="1000" Orientation="Vertical"   Value="{x:Bind Resizer_V1,Mode=TwoWay}"/>

            <!-- Room NotAvailableTimes -->
            <Grid Grid.Column="2" Margin="5" Grid.Row="0" Visibility="{x:Bind WhatY(1);,Mode=OneWay}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Grid Grid.Column="2" Margin="5" Grid.Row="0" Visibility="{x:Bind WhatY(1);,Mode=OneWay}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <InfoBar Grid.Row="0" IsOpen="True" Message="{x:Bind txt(77)}" Severity="Warning"  IsClosable="False">
                    </InfoBar>
                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <DropDownButton x:Name="dd3" Content="{x:Bind gr3_n1,Mode=OneWay}" Click="dd3Click" Grid.Column="0"  Margin="5" />
                        <TextBlock Grid.Column="1" Margin="5" VerticalAlignment="Center" Text="{x:Bind txt(84)}" />
                        <NumberBox x:Name="nb3" Grid.Column="2" HorizontalAlignment="Stretch" Margin="5" PlaceholderText="{x:Bind txt(84)}" Value="{x:Bind gr3_p,Mode=TwoWay}" Minimum="0" Maximum="100" SpinButtonPlacementMode="Inline" />
                    </Grid>
                    <local:DGDataGrid x:Name="gr3" Grid.Row="2" Grid.Column="0" ItemsSource="{x:Bind gr3_is,Mode=OneWay}" ColumnHeaders="{x:Bind gr3_ch,Mode=OneWay}" />
                </Grid>
            </Grid>

        </Grid>
        
        

    </Grid>
</Page>
