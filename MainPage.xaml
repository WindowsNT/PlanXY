<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="WuiFET.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WuiFET"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Loaded="PageLoaded"
    mc:Ignorable="d">


    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <InfoBar x:Name="infoBar" Grid.Row="0" IsOpen="True" Message="" Severity="Success"  IsClosable="False">
            <Grid VerticalAlignment="Stretch">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="{x:Bind txt(1);}" Margin="10,0,0,0" Grid.Column="0" VerticalAlignment="Center"/>
                <TextBlock Text="{x:Bind Loaded_File,Mode=OneWay}" Margin="10,0,0,0" Grid.Column="1" VerticalAlignment="Center"/>
            </Grid>
        </InfoBar>

        <MenuBar HorizontalAlignment="Left" x:Name="menu" Grid.Row="1">
            <MenuBarItem Title="{x:Bind txt(22)}" >
                <MenuFlyoutItem Text="{x:Bind txt(7)}" Click="OnNew" >
                    <MenuFlyoutItem.Icon>
                        <FontIcon Glyph="&#xE78B;"/>
                    </MenuFlyoutItem.Icon>
                    <MenuFlyoutItem.KeyboardAccelerators>
                        <KeyboardAccelerator Key="N" Modifiers="Control"/>
                    </MenuFlyoutItem.KeyboardAccelerators>
                </MenuFlyoutItem>
                <MenuFlyoutSeparator/>
                <MenuFlyoutItem Text="{x:Bind txt(8)}" Click="OnOpen">
                    <MenuFlyoutItem.Icon>
                        <FontIcon Glyph="&#xE8E5;"/>
                    </MenuFlyoutItem.Icon>
                    <MenuFlyoutItem.KeyboardAccelerators>
                        <KeyboardAccelerator Key="O" Modifiers="Control"/>
                    </MenuFlyoutItem.KeyboardAccelerators>
                </MenuFlyoutItem>
                <MenuFlyoutItem Text="MSA-APPS" Click="OnMSAOpen">
                    <MenuFlyoutItem.Icon>
                        <FontIcon Glyph="&#xE8E5;"/>
                    </MenuFlyoutItem.Icon>
                    <MenuFlyoutItem.KeyboardAccelerators>
                        <KeyboardAccelerator Key="M" Modifiers="Control"/>
                    </MenuFlyoutItem.KeyboardAccelerators>
                </MenuFlyoutItem>
                <MenuFlyoutSeparator/>
                <MenuFlyoutItem Text="{x:Bind txt(9)}" Click="OnSave" >
                    <MenuFlyoutItem.Icon>
                        <FontIcon Glyph="&#xE74E;"/>
                    </MenuFlyoutItem.Icon>
                    <MenuFlyoutItem.KeyboardAccelerators>
                        <KeyboardAccelerator Key="S" Modifiers="Control"/>
                    </MenuFlyoutItem.KeyboardAccelerators>
                </MenuFlyoutItem>
                <MenuFlyoutItem Text="{x:Bind txt(10)}" Click="OnSaveAs" >
                    <MenuFlyoutItem.Icon>
                        <FontIcon Glyph="&#xE792;"/>
                    </MenuFlyoutItem.Icon>
                    <MenuFlyoutItem.KeyboardAccelerators>
                        <KeyboardAccelerator Key="S" Modifiers="Control,Shift"/>
                    </MenuFlyoutItem.KeyboardAccelerators>
                </MenuFlyoutItem>
                <MenuFlyoutItem Text="{x:Bind txt(185)}" Click="OnExportFET" >
                    <MenuFlyoutItem.Icon>
                        <FontIcon Glyph="&#xE792;"/>
                    </MenuFlyoutItem.Icon>
                </MenuFlyoutItem>
                <MenuFlyoutSeparator/>
                <MenuFlyoutItem Text="{x:Bind txt(11)}" Click="OnExit" >
                    <MenuFlyoutItem.Icon>
                        <FontIcon Glyph="&#xE711;"/>
                    </MenuFlyoutItem.Icon>
                </MenuFlyoutItem>



            </MenuBarItem>

            <MenuBarItem Title="{x:Bind txt(183)}" x:Name="ExampleMenuRoot">
            </MenuBarItem>


            <MenuBarItem Title="{x:Bind txt(152)}" >
                <MenuFlyoutItem Text="{x:Bind txt(153)}" Click="OnShare" />
                <MenuFlyoutItem Text="{x:Bind txt(154)}" Click="OnJoin" />
            </MenuBarItem>
            
            <MenuBarItem Title="{x:Bind txt(16)}" >
                <MenuFlyoutItem Text="{x:Bind txt(145)}" Click="OnAbout" >
                </MenuFlyoutItem>

            </MenuBarItem>
            
        </MenuBar>

        <TextBlock Grid.Row="2" Text="{x:Bind txt(182),Mode=OneWay}" FontSize="20"  Margin="10,10,0,10" VerticalAlignment="Center" HorizontalAlignment="Center" TextAlignment="Center"/>

    </Grid>
    
</Page>
